/// Stub : pas d'écriture fichier (Web). Retourne null pour utiliser Share.share(text).
Future<String?> writeBalanceCsvToFile(
  String csv,
  String dateDebut,
  String dateFin,
) async =>
    null;
